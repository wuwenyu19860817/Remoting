/**
 * Remoting通信协议 v1.0.0
 *
 * 协议格式 <length> <header length> <header data> <body data> 1 2 3 4 协议分4部分，
 * 含义分别如下
     1、大端4个字节整数，等于2、3、4长度总和
     2、大端4个字节整数，等于3的长度
     3、使用json序列化数据
     4、应用自定义二进制序列化数据
 */
 
 
 
 热加载各个RPC处理器具有特定的版本号
 通过接口注册的RPC处理器固定版本号为0.0.0
 code-version找不到那么进入默认处理器